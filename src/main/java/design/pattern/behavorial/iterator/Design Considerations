Always prefer iterator interface so that you can change the implementation without
affecting client.

Iterator have many applications where a collection is not used but still we want to to give a
sequential access to information for example 